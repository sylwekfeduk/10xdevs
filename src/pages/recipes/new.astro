---
import Layout from "../../layouts/Layout.astro";
import { ToasterStandalone } from "../../components/ui/toaster-standalone";
import { NewRecipeForm } from "../../components/forms/NewRecipeForm";

// TODO: Temporarily disabled for UI testing - Re-enable authentication before production!
// Check if user is authenticated
// if (!Astro.locals.user) {
//   // Redirect to login if not authenticated
//   return Astro.redirect("/login");
// }
---

<Layout title="Create New Recipe">
  <main class="min-h-screen bg-background">
    <div class="container mx-auto max-w-3xl px-4 py-8 sm:px-6 lg:px-8">
      <h1 class="text-3xl font-bold tracking-tight mb-8">Create New Recipe</h1>
      <NewRecipeForm client:load />
    </div>
  </main>
  <ToasterStandalone client:load />
</Layout>
